<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tenseed.shortlink.project.dao.mapper.LinkBrowserStatsMapper">

    <!-- 记录访问系统监控数据 -->
    <insert id="shortLinkBrowserStats" parameterType="com.tenseed.shortlink.project.dao.entity.LinkBrowserStatsDO">
        INSERT INTO t_link_browser_stats (full_short_url,
                                          date,
                                          cnt,
                                          browser,
                                          create_time,
                                          update_time,
                                          del_flag)
        VALUES (#{linkBrowserStats.fullShortUrl},
                #{linkBrowserStats.date},
                #{linkBrowserStats.cnt},
                #{linkBrowserStats.browser},
                NOW(),
                NOW(),
                0)
        ON DUPLICATE KEY UPDATE cnt = cnt + #{linkBrowserStats.cnt};
    </insert>

    <!-- 根据短链接获取指定日期内浏览器监控数据 -->
    <select id="listBrowserStatsByShortLink" parameterType="com.tenseed.shortlink.project.dto.req.ShortLinkStatsReqDTO"
            resultType="java.util.HashMap">
        SELECT tlbs.browser,
               SUM(tlbs.cnt) AS count
        FROM t_link tl
                 INNER JOIN t_link_browser_stats tlbs
                            ON tl.full_short_url = tlbs.full_short_url
        WHERE tlbs.full_short_url = #{param.fullShortUrl}
          AND tl.gid = #{param.gid}
          AND tl.del_flag = '0'
          AND tl.enable_status = #{param.enableStatus}
          AND tlbs.date BETWEEN #{param.startDate} AND #{param.endDate}
        GROUP BY tlbs.full_short_url,
                 tl.gid,
                 tlbs.browser;
    </select>

    <!-- 根据分组获取指定日期内浏览器监控数据 -->
    <select id="listBrowserStatsByGroup" parameterType="com.tenseed.shortlink.project.dto.req.ShortLinkGroupStatsReqDTO"
            resultType="java.util.HashMap">
        SELECT tlbs.browser,
               SUM(tlbs.cnt) AS count
        FROM t_link tl
                 INNER JOIN t_link_browser_stats tlbs
                            ON tl.full_short_url = tlbs.full_short_url
        WHERE tl.gid = #{param.gid}
          AND tl.del_flag = '0'
          AND tl.enable_status = '0'
          AND tlbs.date BETWEEN #{param.startDate} AND #{param.endDate}
        GROUP BY tl.gid,
                 tlbs.browser;
    </select>
</mapper>
