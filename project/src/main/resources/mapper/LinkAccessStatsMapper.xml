<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tenseed.shortlink.project.dao.mapper.LinkAccessStatsMapper">

    <!-- 记录基础访问监控数据 -->
    <insert id="shortLinkStats" parameterType="com.tenseed.shortlink.project.dao.entity.LinkAccessStatsDO">
        INSERT INTO t_link_access_stats (full_short_url,
                                         date,
                                         pv,
                                         uv,
                                         uip,
                                         hour,
                                         weekday,
                                         create_time,
                                         update_time,
                                         del_flag)
        VALUES (#{linkAccessStats.fullShortUrl},
                #{linkAccessStats.date},
                #{linkAccessStats.pv},
                #{linkAccessStats.uv},
                #{linkAccessStats.uip},
                #{linkAccessStats.hour},
                #{linkAccessStats.weekday},
                NOW(),
                NOW(),
                0)
        ON DUPLICATE KEY UPDATE pv  = pv + #{linkAccessStats.pv},
                                uv  = uv + #{linkAccessStats.uv},
                                uip = uip + #{linkAccessStats.uip};
    </insert>

    <!-- 根据短链接获取指定日期内的基础监控数据 -->
    <select id="listStatsByShortLink" parameterType="com.tenseed.shortlink.project.dto.req.ShortLinkStatsReqDTO"
            resultType="com.tenseed.shortlink.project.dao.entity.LinkAccessStatsDO">
        SELECT tlas.date,
               SUM(tlas.pv)  AS pv,
               SUM(tlas.uv)  AS uv,
               SUM(tlas.uip) AS uip
        FROM t_link tl
                 INNER JOIN t_link_access_stats tlas
                            ON tl.full_short_url = tlas.full_short_url
        WHERE tlas.full_short_url = #{param.fullShortUrl}
          AND tl.gid = #{param.gid}
          AND tl.del_flag = '0'
          AND tl.enable_status = '0'
          AND tlas.date BETWEEN #{param.startDate} AND #{param.endDate}
        GROUP BY tlas.full_short_url,
                 tl.gid,
                 tlas.date;
    </select>

    <!-- 根据分组获取指定日期内基础监控数据 -->
    <select id="listStatsByGroup" parameterType="com.tenseed.shortlink.project.dto.req.ShortLinkGroupStatsReqDTO"
            resultType="com.tenseed.shortlink.project.dao.entity.LinkAccessStatsDO">
        SELECT tlas.date,
               SUM(tlas.pv)  AS pv,
               SUM(tlas.uv)  AS uv,
               SUM(tlas.uip) AS uip
        FROM t_link tl
                 INNER JOIN t_link_access_stats tlas
                            ON tl.full_short_url = tlas.full_short_url
        WHERE tl.gid = #{param.gid}
          AND tl.del_flag = '0'
          AND tl.enable_status = '0'
          AND tlas.date BETWEEN #{param.startDate} AND #{param.endDate}
        GROUP BY tl.gid,
                 tlas.date;
    </select>

    <!-- 根据短链接获取指定日期内小时基础监控数据 -->
    <select id="listHourStatsByShortLink" parameterType="com.tenseed.shortlink.project.dto.req.ShortLinkStatsReqDTO"
            resultType="com.tenseed.shortlink.project.dao.entity.LinkAccessStatsDO">
        SELECT tlas.hour,
               SUM(tlas.pv) AS pv
        FROM t_link tl
                 INNER JOIN t_link_access_stats tlas
                            ON tl.full_short_url = tlas.full_short_url
        WHERE tlas.full_short_url = #{param.fullShortUrl}
          AND tl.gid = #{param.gid}
          AND tl.del_flag = '0'
          AND tl.enable_status = '0'
          AND tlas.date BETWEEN #{param.startDate} AND #{param.endDate}
        GROUP BY tlas.full_short_url,
                 tl.gid,
                 tlas.hour;
    </select>

    <!-- 根据分组获取指定日期内小时基础监控数据 -->
    <select id="listHourStatsByGroup" parameterType="com.tenseed.shortlink.project.dto.req.ShortLinkGroupStatsReqDTO"
            resultType="com.tenseed.shortlink.project.dao.entity.LinkAccessStatsDO">
        SELECT tlas.hour,
               SUM(tlas.pv) AS pv
        FROM t_link tl
                 INNER JOIN t_link_access_stats tlas
                            ON tl.full_short_url = tlas.full_short_url
        WHERE tl.gid = #{param.gid}
          AND tl.del_flag = '0'
          AND tl.enable_status = '0'
          AND tlas.date BETWEEN #{param.startDate} AND #{param.endDate}
        GROUP BY tl.gid,
                 tlas.hour;
    </select>

    <!-- 根据短链接获取指定日期内星期基础监控数据 -->
    <select id="listWeekdayStatsByShortLink" parameterType="com.tenseed.shortlink.project.dto.req.ShortLinkStatsReqDTO"
            resultType="com.tenseed.shortlink.project.dao.entity.LinkAccessStatsDO">
        SELECT tlas.weekday,
               SUM(tlas.pv) AS pv
        FROM t_link tl
                 INNER JOIN t_link_access_stats tlas
                            ON tl.full_short_url = tlas.full_short_url
        WHERE tlas.full_short_url = #{param.fullShortUrl}
          AND tl.gid = #{param.gid}
          AND tl.del_flag = '0'
          AND tl.enable_status = '0'
          AND tlas.date BETWEEN #{param.startDate} AND #{param.endDate}
        GROUP BY tlas.full_short_url,
                 tl.gid,
                 tlas.weekday;
    </select>

    <!-- 根据分组获取指定日期内星期基础监控数据 -->
    <select id="listWeekdayStatsByGroup" parameterType="com.tenseed.shortlink.project.dto.req.ShortLinkGroupStatsReqDTO"
            resultType="com.tenseed.shortlink.project.dao.entity.LinkAccessStatsDO">
        SELECT tlas.weekday,
               SUM(tlas.pv) AS pv
        FROM t_link tl
                 INNER JOIN t_link_access_stats tlas
                            ON tl.full_short_url = tlas.full_short_url
        WHERE tl.gid = #{param.gid}
          AND tl.del_flag = '0'
          AND tl.enable_status = '0'
          AND tlas.date BETWEEN #{param.startDate} AND #{param.endDate}
        GROUP BY tl.gid,
                 tlas.weekday;
    </select>
</mapper>
